<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".main"
    android:background="@drawable/background">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:id="@+id/title"
        android:scaleType="fitCenter"
        android:layout_gravity="center|top"
        android:src="@drawable/title"/>

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Igre riječima"
        android:onClick="Igre_rijecimaOnClick"
        android:id="@+id/igre_rijecima"
        android:layout_gravity="center"
        android:layout_below="@+id/title"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Prijedlozi"
        android:onClick="PrijedloziOnClick"
        android:id="@+id/prijedlozi"
        android:layout_gravity="center"
        android:layout_below="@+id/igre_rijecima"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
